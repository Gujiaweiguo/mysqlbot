## 1. Inventory placeholder purposes

- [x] 1.1 Identify all canonical specs under `openspec/specs/**` that still contain the archive placeholder `Purpose` text
- [x] 1.2 Map each placeholder spec to the archived source change that describes its intended capability summary

## 2. Replace placeholder purposes

- [x] 2.1 Update each affected main spec `Purpose` with a concise summary aligned to the archived source change and current requirement body
- [x] 2.2 Leave requirement text and scenarios unchanged while performing the purpose-only cleanup

## 3. Verify canonical spec hygiene

- [x] 3.1 Confirm no archive placeholder `Purpose` text remains under `openspec/specs/**`
- [x] 3.2 Spot-check updated specs to ensure summaries match their current capability requirements
