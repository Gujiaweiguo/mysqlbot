# mySQLBot Full Regression Baseline Report (2026-03-10)

- Report ID: `baseline-2026-03-10`
- Date: 2026-03-10
- Author: Hephaestus
- Change: `full-regression-testing`
- Branch/Commit: `main`

## 1) Scope

- 本次范围：runtime health、前后端质量门禁、fixture/metadata、失败链路可观测性基线。
- 不在范围：完整 UI 自动化回归矩阵、性能压测、全模型供应商覆盖。

## 2) Environment

- Host / OS: Linux (workspace `/opt/code/mysqlbot`)
- Runtime: Docker Compose
- 服务状态：`mysqlbot` = `Up (healthy)`
- Fixture：`demo_sales`（`customers=3`, `orders=4`）

## 3) Gate Execution Summary

| Gate | Name | Result | Key Evidence |
|---|---|---|---|
| G0 | Runtime Health | PASS | `docker compose ps`; login 页面与参数接口 200 |
| G1 | Backend Quality | FAIL | `bash scripts/lint.sh` 失败（mypy not found）；`uv run mypy` 大量类型错误 |
| G2 | Frontend Quality | PASS | `npm run build` exit code 0 |
| G3 | Fixture & Metadata | PASS | `demo_sales` 存在；`core_table/core_field` 与 `customers/orders` 对齐 |
| G4 | Functional Happy Path | BLOCKED | 需稳定认证会话与用例执行上下文（本次未完整跑 API 级 happy-path） |
| G5 | Failure Path | PASS (definition), BLOCKED (live repro) | 已定义 429/transient 验收；本次日志窗口未复现新 429 |
| G6 | Report & Decision | DONE | 本报告已生成 |

## 4) Command Log & Exit Codes

1. `docker compose ps` -> exit code: `0`
2. `curl -L http://119.29.59.128:8000/#/login` -> HTTP `200`
3. `curl http://119.29.59.128:8000/api/v1/system/parameter/login` -> HTTP `200`
4. `bash backend/scripts/lint.sh` -> exit code: `127`
5. `uv run mypy apps common main.py` -> exit code: `1`
6. `npm run build` (frontend) -> exit code: `0`
7. fixture/schema SQL checks -> exit code: `0`
8. metadata sync SQL checks -> exit code: `0`

## 5) Detailed Findings

### G0 Runtime Health

- `docker compose ps` 显示 `mysqlbot` healthy。
- 登录页与登录参数接口均返回 200。

### G1 Backend Quality

- `backend/scripts/lint.sh` 当前依赖系统级 `mypy`，报 `mypy: command not found`。
- 使用 `uv run mypy apps common main.py` 后存在大量存量类型错误。

### G2 Frontend Quality

- `frontend` 构建成功；存在大 chunk warning，但不影响本 gate 通过。

### G3 Fixture & Metadata

- schema 检查：`demo_sales`、`demo_hr`、`public` 均存在。
- `demo_sales` 数据量：`customers=3`、`orders=4`。
- datasource(id=1) 元数据：`num=2/2`，`core_table={customers,orders}`，字段总数 `10`。

### G4 Functional Happy Path

- 本次仅完成环境/数据前置验证，未完成认证态下 API 级用例执行。
- 已在执行手册中定义最小 happy-path 用例集（聚合、分组、状态分布）。

### G5 Failure Path (429 / transient)

- 本次日志窗口未捕获新的 429 记录。
- 已基于既往问题定义了验收标准：受控错误、不中断会话、可追踪日志。

## 6) Risks / Waivers

- 未解决风险：后端质量门禁不通过（脚本与类型错误）。
- Waiver：无（本次不建议 waiver 放行）。

## 7) Release Decision

- Decision: **NO-GO**
- Reason: G1 失败（后端质量门禁未通过），且 G4 未完成完整 happy-path 执行。

## 8) Follow-up Actions

- [ ] 修复 `backend/scripts/lint.sh`（改为 `uv run ...` 或确保依赖注入一致）
- [ ] 制定并执行认证态智能问数 happy-path 自动化回归
- [ ] 增加 429/transient 可控复现方案（mock 或限流压测）
