# mySQLBot Full Regression Baseline Report (2026-03-11)

- Report ID: `baseline-2026-03-11`
- Date: 2026-03-11
- Author: Hephaestus
- Change: `full-regression-testing` continuation (G4/G5 automation + G6 consolidation)
- Branch/Commit: `main@3addc1b2`

## 1) Scope

- 本次范围：G0~G6 全链路回归收口，重点补齐 G4/G5 自动化证据并形成发布决策。
- 不在范围：全量历史 mypy 债务清理、性能压测、跨模型供应商横向对比。

## 2) Environment

- Host / OS: Linux 5.15.0-164-generic x86_64
- Docker / Compose 版本：Docker 29.1.5；Docker Compose v5.0.1
- 服务状态：`mysqlbot` = `Up (healthy)`
- 数据源配置（含 `dbSchema`）：`core_datasource.id=1`, `name=mySQLBot Internal PostgreSQL`, `num=2/2`, fixture schema=`demo_sales`

## 3) Gate Execution Summary

| Gate | Name | Result | Key Evidence |
|---|---|---|---|
| G0 | Runtime Health | PASS | `docker compose ps` healthy；`/#/login`=200；`/api/v1/system/parameter/login`=200 |
| G1 | Backend Quality | FAIL | `bash backend/scripts/lint.sh` 失败，mypy 存量错误仍阻塞 |
| G2 | Frontend Quality | PASS | `npm run build` exit code 0 |
| G3 | Fixture & Metadata | PASS | `demo_sales` 存在；`customers=3`、`orders=4`；`core_table=2`、`core_field=10` |
| G4 | Functional Happy Path | PASS | `docs/regression/evidence/g4-happy-path-20260311-000936.json` |
| G5 | Failure Path | PASS | `docs/regression/evidence/g5-failure-path-20260311-003244.json` |
| G6 | Report & Decision | DONE | 本报告 |

## 4) Command Log & Exit Codes

1. `docker compose ps` -> exit code: `0`
2. `curl -L http://127.0.0.1:8000/#/login` -> HTTP `200`
3. `curl http://127.0.0.1:8000/api/v1/system/parameter/login` -> HTTP `200`
4. `bash backend/scripts/lint.sh` -> exit code: `1`
5. `npm run build` (frontend) -> exit code: `0`
6. `docker exec mysqlbot psql -U root -d sqlbot -t -A -c "select nspname ..."` -> exit code: `0`（`demo_sales`）
7. `docker exec mysqlbot psql -U root -d sqlbot -t -A -c "select count(*) from demo_sales.customers;"` -> exit code: `0`（`3`）
8. `docker exec mysqlbot psql -U root -d sqlbot -t -A -c "select count(*) from demo_sales.orders;"` -> exit code: `0`（`4`）
9. `docker exec mysqlbot psql -U root -d sqlbot -t -A -c "select id,name,num from core_datasource where id=1;"` -> exit code: `0`（`1|mySQLBot Internal PostgreSQL|2/2`）
10. `bash backend/scripts/regression/g5-failure-path-demo-sales.sh --base-url http://127.0.0.1:8000 --provider-host 172.17.0.1` -> exit code: `0`（`[G5 PASS]`）

## 5) Detailed Findings

### G0 Runtime Health

- 运行时服务健康，登录页与登录参数接口均可达。

### G1 Backend Quality

- 当前 `lint.sh` 已支持 changed-scope 与路径归一化，但 gate 仍被历史 mypy 存量问题阻塞。
- 结论：G1 未通过，属于发布阻断项。

### G2 Frontend Quality

- `frontend` 构建成功，存在大 chunk 警告但不影响 gate 判定。

### G3 Fixture & Metadata

- fixture 合约满足：`demo_sales` schema 存在，`customers=3`、`orders=4`。
- metadata 同步满足：`core_table` 包含 `customers/orders`，字段计数 `10`。

### G4 Functional Happy Path

- 三个核心问数用例（聚合、分组、状态分布）全部通过。
- 证据文件：`docs/regression/evidence/g4-happy-path-20260311-000936.json`。

### G5 Failure Path (429 / transient)

- 已实现可控注入并复现：
  - 429 场景：受控报错 + 后续请求可继续。
  - 瞬时 503 场景：首轮失败、次轮恢复，记录调用次数与最终状态。
- 证据文件：`docs/regression/evidence/g5-failure-path-20260311-003244.json`。

## 6) Risks / Waivers

- 未解决风险：G1 仍失败（mypy 历史债务规模大）。
- 运行风险：容器镜像与本地源码在 alembic 版本文件上存在漂移风险（需在 CI 镜像构建链路固化）。
- Waiver：无。

## 7) Release Decision

- Decision: **NO-GO**
- Reason: 按 Stop-on-Fail 规则，G1 为 mandatory gate 且当前失败，尽管 G0/G2/G3/G4/G5 均通过，仍不可放行。

## 8) Follow-up Actions

- [ ] 分批清理后端 mypy 存量问题，先收敛 `apps/mcp`、`common/*` 相关链路。
- [ ] 在 CI 中固定镜像构建来源，避免 alembic 迁移文件缺失/漂移。
- [ ] 将 G4/G5 自动化命令纳入统一回归流水线（与 G0-G3 串联）。
