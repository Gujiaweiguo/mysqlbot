# mySQLBot Full Regression Baseline Report (2026-03-14)

- Report ID: `baseline-2026-03-14`
- Date: 2026-03-14
- Author: Sisyphus
- Change: `full-regression-testing` final verification
- Branch/Commit: `main@3addc1b2` (local changes pending commit)

## 1) Scope

- 本次范围：G0~G6 全链路回归收口，验证所有门禁通过后形成发布决策。
- 不在范围：全量历史 mypy 债务清理、性能压测、跨模型供应商横向对比。

## 2) Environment

- Host / OS: Linux 5.15.0-164-generic x86_64
- Docker / Compose 版本：Docker 29.1.5；Docker Compose v5.0.1
- 服务状态：`mysqlbot` = `Up (healthy)`
- 数据源配置（含 `dbSchema`）：`core_datasource.id=1`, `name=mySQLBot Internal PostgreSQL`, `num=2/2`, fixture schema=`demo_sales`

## 3) Gate Execution Summary

| Gate | Name | Result | Key Evidence |
|---|---|---|---|
| G0 | Runtime Health | PASS | `docker compose ps` healthy；`/#/login`=200；`/api/v1/system/parameter/login`=200 |
| G1 | Backend Quality | PASS | `bash backend/scripts/lint.sh` exit code 0；mypy/ruff 全部通过 |
| G2 | Frontend Quality | PASS | `npm run build` exit code 0 |
| G3 | Fixture & Metadata | PASS | `demo_sales` 存在；`customers=3`、`orders=4`；`core_datasource.id=1` num=2/2 |
| G4 | Functional Happy Path | PASS | `docs/regression/evidence/g4-happy-path-20260314-183700.json` |
| G5 | Failure Path | PASS | `docs/regression/evidence/g5-failure-path-20260314-183837.json` |
| G6 | Report & Decision | DONE | 本报告 |

## 4) Command Log & Exit Codes

1. `docker compose ps` -> exit code: `0` (healthy)
2. `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8000/#/login` -> HTTP `200`
3. `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8000/api/v1/system/parameter/login` -> HTTP `200`
4. `bash backend/scripts/lint.sh` -> exit code: `0` (mypy: 111 files, ruff: all checks passed)
5. `npm run build` (frontend) -> exit code: `0` (built in 32.96s)
6. `docker exec mysqlbot psql ... demo_sales` -> exit code: `0`（schema exists）
7. `docker exec mysqlbot psql ... count(*) from demo_sales.customers` -> `3`
8. `docker exec mysqlbot psql ... count(*) from demo_sales.orders` -> `4`
9. `docker exec mysqlbot psql ... core_datasource where id=1` -> `1|mySQLBot Internal PostgreSQL|2/2`
10. `bash backend/scripts/regression/g4-happy-path-demo-sales.sh` -> exit code: `0`（`[G4 PASS]`）
11. `bash backend/scripts/regression/g5-failure-path-demo-sales.sh` -> exit code: `0`（`[G5 PASS]`）

## 5) Detailed Findings

### G0 Runtime Health

- 运行时服务健康，登录页与登录参数接口均可达。

### G1 Backend Quality

- `lint.sh` 全部通过，mypy 检查 111 个文件无错误，ruff check 与 format 均通过。
- **相比 2026-03-11 基线，G1 已从 FAIL 修复为 PASS。**

### G2 Frontend Quality

- `frontend` 构建成功，存在大 chunk 警告但不影响 gate 判定。

### G3 Fixture & Metadata

- fixture 合约满足：`demo_sales` schema 存在，`customers=3`、`orders=4`。
- metadata 同步满足：`core_datasource.id=1` 包含 `num=2/2`。

### G4 Functional Happy Path

- 三个核心问数用例（聚合、分组、状态分布）全部通过。
- 证据文件：`docs/regression/evidence/g4-happy-path-20260314-183700.json`。

### G5 Failure Path (429 / transient)

- 429 场景与瞬时故障场景均通过验证。
- 证据文件：`docs/regression/evidence/g5-failure-path-20260314-183837.json`。

## 6) Risks / Waivers

- 未解决风险：无。
- 运行风险：容器镜像与本地源码在 alembic 版本文件上存在漂移风险（需在 CI 镜像构建链路固化）。
- Waiver：无。

## 7) Release Decision

- Decision: **GO** ✅
- Reason: 所有强制门禁 G0-G5 全部通过，无阻断项，可以放行。

## 8) Follow-up Actions

- [x] G1 后端质量门禁修复完成
- [ ] 将当前未提交的变更提交并推送
- [ ] 在 CI 中固定镜像构建来源，避免 alembic 迁移文件缺失/漂移
- [ ] 将 G4/G5 自动化命令纳入统一回归流水线（与 G0-G3 串联）
